'use strict';

/* jasmine specs for services go here */
/*
describe('service', function() {
  beforeEach(module('JArentalsApplication.services'));
  
	  var UserService,FaceBookService;
	  
	  beforeEach(inject(function(_UserService_,_FaceBookService_){
			UserService = _UserService_;
			FaceBookService = _FaceBookService_ ;
	  }));
	  
	  describe('UserService', function() {
			it('should have retrieveUserDataFromFaceBook method defined', inject(function() {
			  expect(UserService.retrieveUserDataFromFaceBook).toBeDefined();
			}));
			
			it('should call the method retrieveUserData when retrieveUserDataFromFaceBook is executed ', inject(function() {
			   spyOn(FaceBookService, "retrieveUserData");
			   UserService.retrieveUserDataFromFaceBook();
			   expect(FaceBookService.retrieveUserData).toHaveBeenCalled();
			}));
			
			it('it should return a user object when valid username and password is passed', inject(function() {
				
			   var userName = "";
			   var password = "";
			   var userData = UserService.retrieveUserDataFromFaceBook(userName,password);
			   expect(userData).toBeDefined();
			   
			}));
	  });
  
});
*/